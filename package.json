{
  "name": "apigw-sqs-lambda",
  "version": "0.1.0",
  "bin": {
    "apigw-sqs-lambda": "bin/apigw-sqs-lambda.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "tsc && cdk deploy '*' --require-approval 'never'",
    "d": "tsc && cdk deploy '*' --require-approval 'never'",
    "diff": "tsc && cdk diff"
  },
  "devDependencies": {
    "aws-cdk": "1.93.0",
    "@aws-cdk/assert": "1.93.0",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "@aws-cdk/aws-sns": "1.93.0",
    "@aws-cdk/aws-sns-subscriptions": "1.93.0",
    "@aws-cdk/aws-sqs": "1.93.0",
    "@aws-cdk/core": "1.93.0"
  }
}
